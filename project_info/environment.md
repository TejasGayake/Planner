# Environment — Job Tracker Android App

> Auto-generated by scaffolder agent. Tracks runtime environment state.

## Setup Status

- [x] Project structure scaffolded
- [ ] Android Studio project imported
- [ ] Dependencies synced (./gradlew build)
- [ ] JDK 17 installed and configured
- [ ] Android SDK 34 installed
- [ ] Emulator or device configured for testing

## Prerequisites

1. **JDK 17** — Download from [Adoptium](https://adoptium.net/) or use Android Studio bundled JDK
2. **Android Studio Hedgehog (2023.1.1+)** — Download from [developer.android.com](https://developer.android.com/studio)
3. **Android SDK 34** — Install via SDK Manager in Android Studio
   - SDK Platforms: `Android 14.0 (API 34)`
   - SDK Tools: `Android SDK Build-Tools 34`, `Android Emulator`, `Android SDK Platform-Tools`
4. **Gradle** — Wrapper included; no manual install needed

## Setup Commands (Run from project root)

```powershell
# 1. Verify JDK
java -version
# Expected: openjdk version "17.0.x"

# 2. Verify Android SDK (set ANDROID_HOME if needed)
echo $env:ANDROID_HOME
# Should point to: C:\Users\<user>\AppData\Local\Android\Sdk

# 3. Check Gradle wrapper
./gradlew --version

# 4. Sync project and download dependencies
./gradlew build --refresh-dependencies

# 5. Run lint
./gradlew lintDebug

# 6. Run unit tests
./gradlew testDebugUnitTest

# 7. Build debug APK
./gradlew assembleDebug

# 8. Run UI tests (requires emulator/device)
./gradlew connectedDebugAndroidTest
```

## Dependencies

| Package | Version | Purpose |
|---|---|---|
| org.jetbrains.kotlin:kotlin-stdlib | 1.9.22 | Kotlin standard library |
| androidx.compose:compose-bom | 2024.02.00 | Compose UI bill of materials |
| androidx.room:room-runtime | 2.6.1 | Room database runtime |
| androidx.room:room-ktx | 2.6.1 | Room Kotlin extensions |
| androidx.room:room-compiler | 2.6.1 | Room annotation processor (KSP) |
| com.google.mlkit:text-recognition | 16.0.1 | ML Kit OCR |
| org.jsoup:jsoup | 1.17.2 | HTML scraping |
| androidx.work:work-runtime-ktx | 2.9.0 | WorkManager |
| androidx.navigation:navigation-compose | 2.7.7 | Navigation |
| androidx.lifecycle:lifecycle-viewmodel-compose | 2.7.0 | ViewModel integration |
| org.jetbrains.kotlinx:kotlinx-coroutines-core | 1.8.0 | Coroutines |
| org.jetbrains.kotlinx:kotlinx-serialization-json | 1.6.3 | JSON export |
| com.google.accompanist:accompanist-permissions | 0.34.0 | Runtime permissions |

## Dev Dependencies

| Package | Version | Purpose |
|---|---|---|
| org.junit.jupiter:junit-jupiter | 5.10.2 | Unit testing |
| io.mockk:mockk | 1.13.10 | Mocking |
| app.cash.turbine:turbine | 1.1.0 | Flow testing |
| androidx.compose.ui:ui-test-junit4 | via BOM | Compose UI testing |
| androidx.room:room-testing | 2.6.1 | Room DB testing |
| com.google.truth:truth | 1.4.3 | Assertions |

## Verification

- **Build**: `./gradlew assembleDebug` — should produce `app/build/outputs/apk/debug/app-debug.apk`
- **Unit tests**: `./gradlew testDebugUnitTest` — all tests should pass (green)
- **Lint**: `./gradlew lintDebug` — should show 0 errors (warnings acceptable)
